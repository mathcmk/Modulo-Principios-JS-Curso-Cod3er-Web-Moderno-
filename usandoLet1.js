 var numero = 1
 {
    let numero = 2
    console.log('dentro = ', numero)
 }

 console.log('fora = ', numero)


 // Let tem escopo de bloco, ou seja,
 // só é visível dentro do bloco onde foi declarado. 
 // Já o var tem escopo de função, ou seja, é visível em toda a função onde foi declarado.